# chat nodojs reactjs
``` bach 
# steps to run:
#run backend
> git clone https://github.com/majimenezaquino/chat.git
> cd backend
> npm install 
> npm start

#run frotend /react
> git clone https://github.com/majimenezaquino/chat.git
> cd frotend
> npm install 
> npm start
```
